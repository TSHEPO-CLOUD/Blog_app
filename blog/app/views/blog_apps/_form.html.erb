<%= form_with(model: blog_app, local: true) do |form| %>
  <% if blog_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_app.errors.count, "error") %> prohibited this blog_app from being saved:</h2>

      <ul>
      <% blog_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :topic %>
    <%= form.text_field :topic %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
